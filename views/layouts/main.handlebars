<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="utf-8" />
        <meta
            name="viewport"
            content="width=device-width, initial-scale=1"
        />
        <meta name="google" content="notranslate">
        {{#if styles}}
            {{#each styles}}
                <link rel="stylesheet" href="{{this}}">
            {{/each}}
        {{/if}}
        <link rel="stylesheet" href="/styles/mainstyles.css">
        <title>Checkers From Ivan</title>
    </head>
    <body translate="no">
        {{{body}}}
    </body>
</html>